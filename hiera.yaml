---
version: 5

defaults:
  datadir: data
  data_hash: yaml_data

hierarchy:
  - name: macOS X Major Version
    path: "macosx/%{os.macosx.version.major}.yaml"
  - name: Hostname
    path: "nodes/%{hostname}.yaml"
  - name: Common
    path: common.yaml
